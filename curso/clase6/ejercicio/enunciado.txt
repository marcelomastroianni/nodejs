Dado un json con datos, escribir un servidor con express.json que este leyendo rutas en la url
y que devuelva la lista de items del json, y que cuando recibe un id en una variable en la ruta devuelva solamente el
item que coincida con la coleccion dentro de los objetos json.

1) get /users/

deberia obtener el listado en json de los usuarios

2) get /user/54564564211

deberia devolver el usuario que coincide con el id del usuario

